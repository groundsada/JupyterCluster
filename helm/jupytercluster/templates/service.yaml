apiVersion: v1
kind: Service
metadata:
  name: {{ include "jupytercluster.fullname" . }}
  labels:
    {{- include "jupytercluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jupytercluster.selectorLabels" . | nindent 4 }}

